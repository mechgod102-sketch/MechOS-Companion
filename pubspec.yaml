name: mechos_companion_mobile
description: MechOS Companion Mobile for Android and iPhone.
publish_to: "none"
version: 0.1.1+2

environment:
  sdk: ">=3.9.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.6.0
  flutter_secure_storage: ^10.2.0
  shared_preferences: ^2.5.5
  path_provider: ^2.1.5
  share_plus: ^13.3.0
  cross_file: ^0.3.4+2
  gal: ^2.3.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
